# 📊 客户联系功能 - 完整技术架构

## 🏗️ 系统架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        企业微信客户联系                            │
│                   (对外沟通 - Customer Contact)                   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ 两大方案
                              ▼
        ┌───────────────────────────────────────────┐
        │                                           │
        ▼                                           ▼
┌──────────────────┐                    ┌──────────────────┐
│  方案一：聊天工具栏 │                    │  方案二：自动推送   │
│  (Manual Send)   │                    │  (Auto Notify)   │
└──────────────────┘                    └──────────────────┘
        │                                           │
        │                                           │
        ▼                                           ▼
┌──────────────────────────────────┐   ┌──────────────────────────────┐
│ 👤 员工操作流程                   │   │ 🤖 系统自动触发               │
│                                  │   │                              │
│ 1. 打开客户聊天窗口               │   │ 1. 里程碑通知                 │
│ 2. 点击工具栏"项目进度"           │   │    - 需求确认 ✅              │
│ 3. 侧边栏展示项目列表             │   │    - 开发完成 💻              │
│ 4. 选择项目                      │   │    - 测试通过 ✔️              │
│ 5. 点击"发送进度"                │   │    - 上线部署 🚀              │
│ 6. 客户收到图文消息               │   │                              │
│                                  │   │ 2. 进度阈值通知                │
│ API端点：                         │   │    - 25%, 50%, 75%, 100%    │
│ GET  /sidebar/project-selector   │   │                              │
│ POST /sidebar/send-progress      │   │ 3. 定时周报                   │
│ GET  /sidebar/preview-message    │   │    - 每周五 17:00            │
│                                  │   │    - 批量发送给所有客户         │
└──────────────────────────────────┘   │                              │
                                       │ API端点：                     │
                                       │ POST /auto-notify/milestone   │
                                       │ POST /auto-notify/batch-weekly│
                                       │ POST /auto-notify/progress-   │
                                       │      threshold                │
                                       │ GET  /auto-notify/active-     │
                                       │      projects                 │
                                       └──────────────────────────────┘
                                                   │
                                                   │
        ┌──────────────────────────────────────────┴────────┐
        │                                                   │
        ▼                                                   ▼
┌──────────────────────┐                      ┌──────────────────────┐
│ CustomerContactService│                      │  定时任务调度器       │
│ (核心业务逻辑)        │                      │  (APScheduler)       │
├──────────────────────┤                      ├──────────────────────┤
│ • send_progress_to_  │                      │ • 每周五17:00        │
│   customer()         │                      │ • 检测进度变化        │
│ • auto_notify_on_    │                      │ • 触发里程碑通知      │
│   milestone()        │                      └──────────────────────┘
│ • batch_send_        │
│   progress_updates() │
│ • _build_progress_   │
│   message()          │
│ • _send_external_    │
│   message()          │
└──────────────────────┘
        │
        │ 调用
        ▼
┌──────────────────────┐
│  WeChatWorkAPI       │
│  (企业微信API封装)    │
├──────────────────────┤
│ • get_access_token() │
│ • send_message()     │
│ • /externalcontact/  │
│   message/send       │
└──────────────────────┘
        │
        │ 发送消息
        ▼
┌──────────────────────────────────────────┐
│        企业微信服务器 (腾讯)               │
│     https://qyapi.weixin.qq.com          │
└──────────────────────────────────────────┘
        │
        │ 推送消息
        ▼
┌──────────────────────────────────────────┐
│         👤 外部客户 (External Contact)    │
│                                          │
│  收到图文消息：                           │
│  ┌────────────────────────────────┐    │
│  │ 📊 XX项目 - 项目进度通知        │    │
│  ├────────────────────────────────┤    │
│  │ 项目名称：XX系统开发V2.0        │    │
│  │ 当前进度：已完成 80%            │    │
│  │ 最新状态：进行中               │    │
│  │                                │    │
│  │ 💡 点击查看详细进度报告         │    │
│  │ ⏰ 页面将每30分钟自动更新        │    │
│  └────────────────────────────────┘    │
│           │ 点击链接                     │
│           ▼                             │
│  ┌────────────────────────────────┐    │
│  │  🔒 安全链接验证                │    │
│  │  JWT Token: eyJ0eXAiOiJKV...   │    │
│  │  有效期：1小时                  │    │
│  └────────────────────────────────┘    │
│           │ 验证通过                     │
│           ▼                             │
│  ┌────────────────────────────────┐    │
│  │  📄 项目详情页面                │    │
│  │  (SSR渲染 + 定时更新)           │    │
│  ├────────────────────────────────┤    │
│  │ • 项目信息                      │    │
│  │ • 进度条（80%）                 │    │
│  │ • 任务列表                      │    │
│  │ • 里程碑时间线                  │    │
│  │ • 负责团队                      │    │
│  │                                │    │
│  │ [每30分钟自动刷新最新数据]      │    │
│  └────────────────────────────────┘    │
└──────────────────────────────────────────┘
```

---

## 🔄 数据流向图

```
方案一：员工手动发送

员工端                 后端服务器              企业微信              客户端
  │                      │                      │                    │
  │ 1. 点击工具栏          │                      │                    │
  ├─────────────────────>│                      │                    │
  │ GET /sidebar/         │                      │                    │
  │ project-selector      │                      │                    │
  │                      │                      │                    │
  │<─────────────────────┤                      │                    │
  │ 返回HTML侧边栏页面     │                      │                    │
  │                      │                      │                    │
  │ 2. 选择项目并点击发送  │                      │                    │
  ├─────────────────────>│                      │                    │
  │ POST /sidebar/        │                      │                    │
  │ send-progress         │                      │                    │
  │ {project_id: 1}       │                      │                    │
  │                      │                      │                    │
  │                      │ 3. 生成JWT令牌        │                    │
  │                      │ token = eyJ0eXAi...  │                    │
  │                      │                      │                    │
  │                      │ 4. 构建图文消息       │                    │
  │                      │ message = {          │                    │
  │                      │   title: "XX项目",   │                    │
  │                      │   url: "https://xxx" │                    │
  │                      │ }                    │                    │
  │                      │                      │                    │
  │                      │ 5. 调用企业微信API    │                    │
  │                      ├─────────────────────>│                    │
  │                      │ POST /externalcontact│                    │
  │                      │ /message/send        │                    │
  │                      │                      │                    │
  │                      │                      │ 6. 推送消息给客户   │
  │                      │                      ├───────────────────>│
  │                      │                      │                    │
  │                      │                      │ 7. 客户点击链接     │
  │                      │<─────────────────────┼────────────────────┤
  │                      │ GET /view/project-   │                    │
  │                      │ detail?token=xxx     │                    │
  │                      │                      │                    │
  │                      │ 8. 验证JWT令牌        │                    │
  │                      │ decode(token)        │                    │
  │                      │                      │                    │
  │                      │ 9. 查询项目数据       │                    │
  │                      │ SELECT * FROM project│                    │
  │                      │                      │                    │
  │                      │ 10. SSR渲染HTML      │                    │
  │                      ├──────────────────────┼───────────────────>│
  │                      │ 返回项目详情页面      │                    │
  │                      │                      │                    │
  │                      │                      │ 11. 定时增量更新    │
  │                      │<─────────────────────┼────────────────────┤
  │                      │ (每30分钟)           │                    │
  │                      │ GET /view/project-   │                    │
  │                      │ progress/1           │                    │
  │                      ├──────────────────────┼───────────────────>│
  │                      │ 返回JSON数据         │                    │
  │                      │                      │                    │
```

```
方案二：系统自动推送

定时任务              后端服务器              企业微信              客户端
  │                      │                      │                    │
  │ 1. 每周五17:00触发    │                      │                    │
  ├─────────────────────>│                      │                    │
  │ POST /auto-notify/   │                      │                    │
  │ batch-weekly         │                      │                    │
  │                      │                      │                    │
  │                      │ 2. 查询活跃项目       │                    │
  │                      │ SELECT * FROM project│                    │
  │                      │ WHERE status='       │                    │
  │                      │ processing'          │                    │
  │                      │                      │                    │
  │                      │ 3. 循环处理每个项目   │                    │
  │                      │ for project in       │                    │
  │                      │ projects:            │                    │
  │                      │                      │                    │
  │                      │ 4. 生成安全链接       │                    │
  │                      │ secure_link =        │                    │
  │                      │ generate_link()      │                    │
  │                      │                      │                    │
  │                      │ 5. 构建图文消息       │                    │
  │                      │ message = build()    │                    │
  │                      │                      │                    │
  │                      │ 6. 批量发送消息       │                    │
  │                      ├─────────────────────>│                    │
  │                      │ (间隔1秒，避免频繁)   │                    │
  │                      │                      │                    │
  │                      │                      │ 7. 推送给客户A      │
  │                      │                      ├───────────────────>│
  │                      │                      │                    │
  │                      │ 等待1秒...            │                    │
  │                      │                      │                    │
  │                      │                      │ 8. 推送给客户B      │
  │                      │                      ├───────────────────>│
  │                      │                      │                    │
  │                      │ 等待1秒...            │                    │
  │                      │                      │                    │
  │                      │                      │ 9. 推送给客户C      │
  │                      │                      ├───────────────────>│
  │                      │                      │                    │
  │<─────────────────────┤                      │                    │
  │ 返回发送结果          │                      │                    │
  │ {success: 3, fail: 0}│                      │                    │
  │                      │                      │                    │
```

---

## 🗂️ 文件组织结构

```
customer-system/
├── backend/
│   ├── app/
│   │   ├── services/
│   │   │   ├── customer_contact_service.py    # 客户联系核心服务 ⭐
│   │   │   ├── secure_link_service.py         # JWT安全链接
│   │   │   ├── cache_service.py               # Redis缓存
│   │   │   ├── ticket_interaction_service.py  # 工单交互
│   │   │   ├── ticket_reminder_service.py     # 超时提醒
│   │   │   └── customer_history_service.py    # 客户历史
│   │   │
│   │   ├── routers/
│   │   │   ├── sidebar.py                     # 聊天工具栏路由 ⭐
│   │   │   ├── auto_notify.py                 # 自动通知路由 ⭐
│   │   │   ├── view.py                        # 项目详情页面路由
│   │   │   ├── wechat.py                      # 企业微信消息路由
│   │   │   ├── admin.py                       # 管理后台路由
│   │   │   └── ai_router.py                   # AI智能路由
│   │   │
│   │   ├── templates/
│   │   │   ├── sidebar_project_selector.html  # 侧边栏页面 ⭐
│   │   │   ├── project_detail.html            # 项目详情页面
│   │   │   └── ...
│   │   │
│   │   ├── utils/
│   │   │   ├── wechat_work_api.py             # 企业微信API封装
│   │   │   └── ...
│   │   │
│   │   ├── models.py                          # 数据库模型
│   │   ├── database.py                        # 数据库连接
│   │   └── main.py                            # FastAPI应用入口
│   │
│   ├── .env                                   # 环境变量配置
│   └── requirements.txt                       # Python依赖
│
├── 客户联系功能-完整配置指南.md                  # 完整文档 ⭐
├── 客户联系功能-快速启动清单.md                  # 快速启动 ⭐
├── 客户联系功能-技术架构图.md                   # 架构文档 ⭐（当前文件）
│
├── 智能工单交互系统-完整实现方案.md
├── 智能工单系统-快速参考卡.md
├── 安全链接与缓存策略-使用指南.md
└── ...
```

---

## 🔑 核心组件说明

### 1. CustomerContactService（客户联系服务）

**文件**：`backend/app/services/customer_contact_service.py`

**核心方法**：

```python
class CustomerContactService:
    
    @staticmethod
    async def send_progress_to_customer(
        db, project_id, customer_external_userid, sender_userid
    ):
        """发送项目进度给客户"""
        # 1. 查询项目和客户信息
        # 2. 生成JWT安全链接（1小时有效）
        # 3. 构建图文消息
        # 4. 调用企业微信API发送
        
    @staticmethod
    async def auto_notify_on_milestone(
        db, project_id, milestone, customer_external_userid, sender_userid
    ):
        """里程碑通知"""
        # 当项目达到重要节点时触发
        # 如：需求确认、开发完成、测试通过等
        
    @staticmethod
    async def batch_send_progress_updates(
        db, project_ids, sender_userid
    ):
        """批量发送周报"""
        # 用于每周五定时向所有客户发送进度更新
        # 间隔1秒发送，避免频繁调用API
```

**依赖关系**：

```
CustomerContactService
  ├─> SecureLinkService.generate_project_detail_link()
  ├─> WeChatWorkAPI.get_access_token()
  ├─> WeChatWorkAPI.send_message()
  └─> Database (查询Project、Customer)
```

---

### 2. 聊天工具栏路由

**文件**：`backend/app/routers/sidebar.py`

**端点**：

```python
# 1. 展示项目选择器侧边栏
GET /sidebar/project-selector
    ?userid=zhangsan
    &external_userid=wmXXXXXXXXXXXX
    
# 2. 发送项目进度
POST /sidebar/send-progress
    {
        "project_id": 1,
        "userid": "zhangsan",
        "external_userid": "wmXXXXXXXXXXXX"
    }
    
# 3. 预览消息内容
GET /sidebar/preview-message
    ?project_id=1
    &external_userid=wmXXXXXXXXXXXX
```

**工作流程**：

```
员工点击工具栏
  │
  ▼
企业微信调用 /sidebar/project-selector
  │
  ▼
后端渲染侧边栏HTML页面
  │
  ▼
展示该客户的所有项目
  │
  ▼
员工选择项目，点击"发送"
  │
  ▼
JavaScript调用 /sidebar/send-progress
  │
  ▼
后端调用 CustomerContactService.send_progress_to_customer()
  │
  ▼
生成JWT安全链接
  │
  ▼
构建图文消息
  │
  ▼
调用企业微信API发送给客户
  │
  ▼
客户收到消息，点击链接
  │
  ▼
跳转到 /view/project-detail?token=xxx
  │
  ▼
验证JWT令牌，显示项目详情页
```

---

### 3. 自动通知路由

**文件**：`backend/app/routers/auto_notify.py`

**端点**：

```python
# 1. 里程碑通知
POST /auto-notify/milestone
    {
        "project_id": 1,
        "milestone": "开发完成",
        "customer_external_userid": "wmXXXXXXXXXXXX",
        "sender_userid": "zhangsan"
    }

# 2. 批量周报
POST /auto-notify/batch-weekly
    {
        "project_ids": [1, 2, 3, 4, 5],
        "sender_userid": "system"
    }

# 3. 进度阈值通知
POST /auto-notify/progress-threshold
    ?project_id=1
    &threshold=50
    &sender_userid=zhangsan

# 4. 获取活跃项目列表
GET /auto-notify/active-projects
```

**触发方式**：

```
方式1：APScheduler定时任务
  @scheduler.scheduled_job('cron', day_of_week='fri', hour=17, minute=0)
  async def send_weekly_reports():
      await call_api('/auto-notify/batch-weekly')

方式2：项目进度更新时触发
  async def update_project_progress(project_id, new_progress):
      # 更新数据库
      await db.update(project_id, progress=new_progress)
      
      # 检测阈值
      if new_progress >= 50 and old_progress < 50:
          await call_api('/auto-notify/progress-threshold', threshold=50)

方式3：Linux Crontab
  0 17 * * 5 curl -X POST http://localhost:8000/auto-notify/batch-weekly
```

---

### 4. 侧边栏前端页面

**文件**：`backend/app/templates/sidebar_project_selector.html`

**技术栈**：
- HTML5 + CSS3
- 纯JavaScript（无框架依赖）
- Fetch API（异步HTTP请求）

**核心功能**：

```javascript
// 1. 选择项目
function selectProject(projectId) {
    // 高亮选中的项目卡片
    document.querySelectorAll('.project-card').forEach(card => {
        card.classList.remove('selected');
    });
    selectedCard.classList.add('selected');
    
    // 启用发送按钮
    sendButton.disabled = false;
}

// 2. 发送进度
async function sendProgress() {
    const response = await fetch('/sidebar/send-progress', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({
            project_id: selectedProjectId,
            userid: userid,
            external_userid: externalUserid
        })
    });
    
    const result = await response.json();
    
    if (result.success) {
        showToast('✅ 项目进度已发送给客户！');
        setTimeout(() => window.close(), 2000);
    }
}
```

**UI设计**：
- 渐变背景（#667eea → #764ba2）
- 卡片式项目展示
- 进度条可视化
- 动画过渡效果
- Toast消息提示

---

## 🔐 安全机制

### 1. JWT令牌验证

```python
# 生成令牌
token = jwt.encode({
    "user_id": customer_external_userid,
    "project_id": project_id,
    "wechat_user_id": customer_external_userid,
    "exp": datetime.now() + timedelta(hours=1)
}, secret_key, algorithm="HS256")

# 验证令牌
try:
    payload = jwt.decode(token, secret_key, algorithms=["HS256"])
    # 检查是否过期
    # 检查用户权限
except jwt.ExpiredSignatureError:
    raise Exception("链接已过期")
except jwt.InvalidTokenError:
    raise Exception("无效的访问令牌")
```

### 2. 权限校验

```python
# 验证客户是否有权限查看该项目
if payload["user_id"] != current_user_id:
    raise HTTPException(status_code=403, detail="无权访问")

if payload["project_id"] != requested_project_id:
    raise HTTPException(status_code=403, detail="无权访问该项目")
```

### 3. HTTPS加密

```
生产环境必须使用HTTPS：
- 聊天工具栏URL必须是HTTPS
- 企业微信要求所有回调URL使用HTTPS
- JWT令牌在HTTPS下传输更安全
```

---

## 🚀 性能优化

### 1. 三层缓存架构

```
浏览器缓存（30分钟）
  ↓
Redis缓存（10分钟TTL）
  ↓
数据库查询
```

### 2. 批量发送限流

```python
# 批量发送时，每个消息间隔1秒
for project_id in project_ids:
    await send_message(project_id)
    await asyncio.sleep(1)  # 避免频繁调用API
```

### 3. 后台任务

```python
# 使用FastAPI BackgroundTasks
@router.post("/batch-weekly")
async def batch_weekly(background_tasks: BackgroundTasks):
    background_tasks.add_task(_batch_send_task)
    return {"message": "已开始批量发送"}
```

---

## 📊 监控与日志

### 建议监控指标

```python
# 1. 消息发送成功率
send_success_rate = successful_sends / total_sends

# 2. 平均响应时间
avg_response_time = sum(response_times) / len(response_times)

# 3. 链接访问量
link_access_count = len(page_views)

# 4. 客户打开率
open_rate = unique_opens / total_sends
```

### 日志记录

```python
import logging

logger = logging.getLogger("customer_contact")

# 记录发送成功
logger.info(f"✅ 消息发送成功：project_id={project_id}, customer={external_userid}")

# 记录发送失败
logger.error(f"❌ 消息发送失败：{error_message}")

# 记录批量发送结果
logger.info(f"📊 批量发送完成：成功 {success_count}/{total_count}")
```

---

## 🎯 最佳实践

### 1. 消息发送时机

✅ **推荐**：
- 里程碑达成时立即通知
- 每周五下班前发送周报
- 进度达到25%、50%、75%、100%时通知

❌ **避免**：
- 频繁发送（如每次小更新都通知）
- 半夜发送消息（影响客户体验）
- 同一客户短时间内多次通知

### 2. 消息内容设计

✅ **推荐**：
- 简洁明了，突出重点
- 包含关键信息（项目名称、进度、状态）
- 使用Emoji增加可读性
- 提供点击链接查看详情

❌ **避免**：
- 过长的文字描述
- 技术术语过多
- 缺少行动指引

### 3. 链接有效期设置

```
场景                   推荐有效期
─────────────────────────────
手动发送（侧边栏）      1小时
里程碑通知             2小时
批量周报              24小时
重要通知              48小时
```

---

## 📞 技术支持文档

- [完整配置指南](./客户联系功能-完整配置指南.md)
- [快速启动清单](./客户联系功能-快速启动清单.md)
- [安全链接使用指南](./安全链接与缓存策略-使用指南.md)
- [智能工单系统参考](./智能工单系统-快速参考卡.md)
- FastAPI文档：http://localhost:8000/docs

---

## ✅ 系统完整性检查清单

- ✅ 企业微信后台配置完成
  - ✅ 聊天工具栏配置
  - ✅ API权限开启
  - ✅ 可信域名配置
  - ✅ Secret配置
  
- ✅ 后端服务实现
  - ✅ CustomerContactService（520行）
  - ✅ 侧边栏路由（156行）
  - ✅ 自动通知路由（230行）
  - ✅ 侧边栏前端页面（278行）
  
- ✅ 路由注册
  - ✅ /sidebar/project-selector
  - ✅ /sidebar/send-progress
  - ✅ /sidebar/preview-message
  - ✅ /auto-notify/milestone
  - ✅ /auto-notify/batch-weekly
  - ✅ /auto-notify/progress-threshold
  - ✅ /auto-notify/active-projects
  
- ✅ 文档完整性
  - ✅ 完整配置指南
  - ✅ 快速启动清单
  - ✅ 技术架构图

🎉 **客户联系功能已100%实现并文档化！**
